<?xml version="1.0" encoding="UTF-8"?>
<ehcache>
    <!-- 전체 캐시 기존 설정-->
    <!--
    maxElementsInMemory : 메모리에 캐싱되는 최대 객체의 수
    eternal : 캐시 시간설정 관련 설정 / ture : (TTL)을 무시하고 캐시가 삭제되지 않음
    timeToIdleSeconds : 일정시간 지났을때, 캐시는 더 유지할지 설정 (기본값 : 0)
    timeToLiveSeconds : 최소 캐시 유지시간
    diskExpiryThreadIntervalSeconds : 메모리에 부족한 캐시 데이터는 하드디스크 저장함,
    캐싱 시간이 만료된 캐시들을 하드디스크에 제거하는 쓰레드가 동작하는 시간간격
    memoryStoreEvictionPolicy : 메모리에 캐싱되는 최대 객체의 수가 도달했을때, 캐시 삭제하는 방법
    -->
    <defaultCache
            maxElementsInMemory="10000"
            eternal="false"
            timeToIdleSeconds="1200"
            timeToLiveSeconds="1200"
            diskExpiryThreadIntervalSeconds="1200"
            memoryStoreEvictionPolicy="LRU"/>
</ehcache>